# Copy to .env (same folder). Never commit this file.
# Needed because JackFredLib + WhereIsIt download from GitHub Packages
# (authentication is required even for public packages).
GITHUB_ACTOR=your-github-username
GITHUB_TOKEN=your-personal-access-token-with-read-packages
